"""Owning the terminal, and the screen the agent thinks it owns.

The PTY that runs the agent, and the menu that floats above it. Nothing in
here knows which agent is running or why we would type at it.
"""
